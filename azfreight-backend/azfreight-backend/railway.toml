[build]
buildCommand = "npm install && prisma generate && nest build"

[deploy]
startCommand = "prisma db push --accept-data-loss && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
